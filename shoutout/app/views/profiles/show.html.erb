<h1>Your Profile</h1>

<%= @profile.username %>
<%= @profile.email %>
<% @profile.shouts.each do |s| %>
<li>
  <%= link_to "#{s.title}" %>
</li>
<% end %>

<%= button_to "Delete #{@profile.username}", profile_path(@profile), :method => :delete  %>

